name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    env:
      ZIG_LOCAL_CACHE_DIR: ${{ matrix.zig-local-cache-dir }}
      ZIG_GLOBAL_CACHE_DIR: ${{ matrix.zig-global-cache-dir }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS aarch64
            runner: namespace-profile-libghostty-mac
            zig-local-cache-dir: /Users/runner/zig/local-cache
            zig-global-cache-dir: /Users/runner/zig/global-cache
          - name: Linux aarch64
            runner: namespace-profile-linux-aarch64-sm
            zig-local-cache-dir: /zig/local-cache
            zig-global-cache-dir: /zig/global-cache
          - name: Linux x86_64
            runner: namespace-profile-linux-x86-sm
            zig-local-cache-dir: /zig/local-cache
            zig-global-cache-dir: /zig/global-cache
    steps:
      - uses: actions/checkout@v4

      - name: Setup Namespace cache
        uses: namespacelabs/nscloud-cache-action@15799a6b54e5765f85b2aac25b3f0df43ed571c0 # v1.4.3
        if: runner.os == 'Linux'
        with:
          path: |
            /zig

      - name: Setup Namespace cache
        uses: namespacelabs/nscloud-cache-action@15799a6b54e5765f85b2aac25b3f0df43ed571c0 # v1.4.3
        if: runner.os == 'macOS'
        with:
          cache: |
            xcode
          path: |
            /Users/runner/zig

      - name: Install Nix
        uses: cachix/install-nix-action@8aa03977d8d733052d78f4e008a241fd1dbf36b3 # v31.10.6
        if: runner.os == 'Linux'
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main
        if: runner.os == 'macOS'
        with:
          determinate: true

      - name: Build package derivation
        run: nix build .#default --print-build-logs

      - name: Run flake checks
        run: nix flake check --print-build-logs

      - name: Check pkg-config path
        if: runner.os == 'Linux'
        run: |
          nix develop --command cargo build -p libghostty-vt-sys
          PKG_CONFIG_PATH=$(find target/debug/build -path '*/out/ghostty-install/share/pkgconfig' | head -1)
          test -n "$PKG_CONFIG_PATH"
          export PKG_CONFIG_PATH
          nix develop --command cargo check -p libghostty-vt-sys --features pkg-config
